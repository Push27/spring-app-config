eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761}
okta:
  oauth2:
    issuer: https://dev-83227211.okta.com/oauth2/default
    audience: api://default
